# Jido Hive Agent Coordinator

Workflow-scoped spawn, handoff, cancel, and supervise contracts for governed
multi-agent coordination. Records carry refs only and use memory-default store
posture unless a durable adapter is explicitly preflighted.

## Persistence Documentation

See `docs/persistence.md` for tiers, defaults, adapters, unsupported selections, config examples, restart claims, durability claims, debug sidecar behavior, redaction guarantees, migration or preflight behavior, and no-bypass scope when applicable.
