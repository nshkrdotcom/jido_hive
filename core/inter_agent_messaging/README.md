# Jido Hive Inter-Agent Messaging

Bounded inter-agent message contracts. Message routing validates tenant,
installation, authority, recipient declaration, fanout, token, byte, turn, and
wall-clock budgets before returning any trace or projection record.
